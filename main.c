//#include "main.h"
#include <stdio.h>

int main(void) {

    long double roots[2] = { 0, 0 };

    long double a = 7.7, b = 3.952, c = -2.10;
    // long double a = 0, b = 5.23, c = 9.1;
    // long double a = 1, b = 0, c = -2;
    // long double a = 3.63, b = 7.27, c = 0;
    // long double a = 3.63, b = 7.27, c = 25.5;

    //testStrictSolveQE(a, b, c, roots, LENGTH);


   /* if (strictSolveQE(a, b, c, roots, 2))
        printf("X1: %Lf; X2: %Lf\n", roots[0], roots[1]);
    else
        printf("Cannot calculate roots or put them to answer-array.\n");
    */

    // solverQES();


//	printf("%d", solveQE(roots));

    printf("Hello to everyone!");

    return 1;
}
